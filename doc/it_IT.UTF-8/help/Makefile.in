# Makefile.in generated by automake 1.16.5 from Makefile.am.
# @configure_input@

# Copyright (C) 1994-2021 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

#  This file is a template for setting the platform-specific LANG
#  variable for a locale.

# build and install SDL files for the help system

# This unit installs the various data that goes with the help SDL files
# like the graphics and the hf files.

VPATH = @srcdir@
am__is_gnu_make = { \
  if test -z '$(MAKELEVEL)'; then \
    false; \
  elif test -n '$(MAKE_HOST)'; then \
    true; \
  elif test -n '$(MAKE_VERSION)' && test -n '$(CURDIR)'; then \
    true; \
  else \
    false; \
  fi; \
}
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = doc/it_IT.UTF-8/help
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/ax_pthread.m4 \
	$(top_srcdir)/m4/compiler_flag_chk.m4 \
	$(top_srcdir)/m4/host-cpu-c-abi.m4 $(top_srcdir)/m4/iconv.m4 \
	$(top_srcdir)/m4/lib-ld.m4 $(top_srcdir)/m4/lib-link.m4 \
	$(top_srcdir)/m4/lib-prefix.m4 $(top_srcdir)/m4/libtool.m4 \
	$(top_srcdir)/m4/ltoptions.m4 $(top_srcdir)/m4/ltsugar.m4 \
	$(top_srcdir)/m4/ltversion.m4 $(top_srcdir)/m4/lt~obsolete.m4 \
	$(top_srcdir)/m4/tcl.m4 $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
DIST_COMMON = $(srcdir)/Makefile.am $(dist_appbuilder_DATA) \
	$(dist_appmanager_DATA) $(dist_calculator_DATA) \
	$(dist_calendar_DATA) $(dist_cmngraph_DATA) \
	$(dist_creatact_DATA) $(dist_filemgr_DATA) $(dist_fpanel_DATA) \
	$(dist_help4help_DATA) $(dist_hfdir_DATA) $(dist_iconed_DATA) \
	$(dist_infomgr_DATA) $(dist_intromgr_DATA) \
	$(dist_libdtprint_DATA) $(dist_loginmgr_DATA) \
	$(dist_mailer_DATA) $(dist_printmgr_DATA) \
	$(dist_prnsetup_DATA) $(dist_sdlhelp_DATA) \
	$(dist_stylemgr_DATA) $(dist_terminal_DATA) \
	$(dist_textedit_DATA) $(am__DIST_COMMON)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/include/cde_config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_P = $(am__v_P_@AM_V@)
am__v_P_ = $(am__v_P_@AM_DEFAULT_V@)
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_@AM_V@)
am__v_GEN_ = $(am__v_GEN_@AM_DEFAULT_V@)
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_@AM_V@)
am__v_at_ = $(am__v_at_@AM_DEFAULT_V@)
am__v_at_0 = @
am__v_at_1 = 
SOURCES =
DIST_SOURCES =
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(appbuilderdir)" \
	"$(DESTDIR)$(appmanagerdir)" "$(DESTDIR)$(calculatordir)" \
	"$(DESTDIR)$(calendardir)" "$(DESTDIR)$(cmngraphdir)" \
	"$(DESTDIR)$(creatactdir)" "$(DESTDIR)$(filemgrdir)" \
	"$(DESTDIR)$(fpaneldir)" "$(DESTDIR)$(help4helpdir)" \
	"$(DESTDIR)$(hfdirdir)" "$(DESTDIR)$(iconeddir)" \
	"$(DESTDIR)$(infomgrdir)" "$(DESTDIR)$(intromgrdir)" \
	"$(DESTDIR)$(libdtprintdir)" "$(DESTDIR)$(loginmgrdir)" \
	"$(DESTDIR)$(mailerdir)" "$(DESTDIR)$(printmgrdir)" \
	"$(DESTDIR)$(prnsetupdir)" "$(DESTDIR)$(sdlhelpdir)" \
	"$(DESTDIR)$(stylemgrdir)" "$(DESTDIR)$(terminaldir)" \
	"$(DESTDIR)$(texteditdir)"
DATA = $(dist_appbuilder_DATA) $(dist_appmanager_DATA) \
	$(dist_calculator_DATA) $(dist_calendar_DATA) \
	$(dist_cmngraph_DATA) $(dist_creatact_DATA) \
	$(dist_filemgr_DATA) $(dist_fpanel_DATA) \
	$(dist_help4help_DATA) $(dist_hfdir_DATA) $(dist_iconed_DATA) \
	$(dist_infomgr_DATA) $(dist_intromgr_DATA) \
	$(dist_libdtprint_DATA) $(dist_loginmgr_DATA) \
	$(dist_mailer_DATA) $(dist_printmgr_DATA) \
	$(dist_prnsetup_DATA) $(dist_sdlhelp_DATA) \
	$(dist_stylemgr_DATA) $(dist_terminal_DATA) \
	$(dist_textedit_DATA)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
am__DIST_COMMON = $(srcdir)/Makefile.in \
	$(top_srcdir)/doc/common/help/sdl-data.am \
	$(top_srcdir)/doc/common/help/sdl-docs.am \
	$(top_srcdir)/programs/localized/templates/Italian.am
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AM_DEFAULT_VERBOSITY = @AM_DEFAULT_VERBOSITY@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BDFTOPCF = @BDFTOPCF@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CDE_CONFIGURATION_TOP = @CDE_CONFIGURATION_TOP@
CDE_INSTALLATION_TOP = @CDE_INSTALLATION_TOP@
CDE_LOGFILES_TOP = @CDE_LOGFILES_TOP@
CDE_USER_TOP = @CDE_USER_TOP@
CDE_VERSION_DEV = @CDE_VERSION_DEV@
CDE_VERSION_MAJOR = @CDE_VERSION_MAJOR@
CDE_VERSION_MICRO = @CDE_VERSION_MICRO@
CDE_VERSION_MINOR = @CDE_VERSION_MINOR@
CFLAGS = @CFLAGS@
CP = @CP@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CPP_PROGRAM = @CPP_PROGRAM@
CSA_INCDIR = @CSA_INCDIR@
CSCOPE = @CSCOPE@
CTAGS = @CTAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DLLTOOL = @DLLTOOL@
DSYMUTIL = @DSYMUTIL@
DTCLIENTLIBS = @DTCLIENTLIBS@
DTI_INCDIR = @DTI_INCDIR@
DT_INCDIR = @DT_INCDIR@
DUMPBIN = @DUMPBIN@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
ETAGS = @ETAGS@
EXEEXT = @EXEEXT@
FGREP = @FGREP@
FILECMD = @FILECMD@
GENCAT = @GENCAT@
GENCPP = @GENCPP@
GREP = @GREP@
GZIP = @GZIP@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
JPEGLIB = @JPEGLIB@
KSH = @KSH@
LD = @LD@
LDFLAGS = @LDFLAGS@
LEX = @LEX@
LEXLIB = @LEXLIB@
LEX_OUTPUT_ROOT = @LEX_OUTPUT_ROOT@
LIBCSA = @LIBCSA@
LIBHELP = @LIBHELP@
LIBICONV = @LIBICONV@
LIBMMDB = @LIBMMDB@
LIBMRM = @LIBMRM@
LIBOBJS = @LIBOBJS@
LIBPRINT = @LIBPRINT@
LIBS = @LIBS@
LIBSEARCH = @LIBSEARCH@
LIBSVC = @LIBSVC@
LIBTERM = @LIBTERM@
LIBTOOL = @LIBTOOL@
LIBTT = @LIBTT@
LIBWIDGET = @LIBWIDGET@
LIBXIN = @LIBXIN@
LIPO = @LIPO@
LN_S = @LN_S@
LTLIBICONV = @LTLIBICONV@
LTLIBOBJS = @LTLIBOBJS@
LT_SYS_LIBRARY_PATH = @LT_SYS_LIBRARY_PATH@
M4 = @M4@
MAKEINFO = @MAKEINFO@
MANIFEST_TOOL = @MANIFEST_TOOL@
MKDIR_P = @MKDIR_P@
MKFONTDIR = @MKFONTDIR@
NM = @NM@
NMEDIT = @NMEDIT@
OBJDUMP = @OBJDUMP@
OBJEXT = @OBJEXT@
ONSGMLS = @ONSGMLS@
OTOOL = @OTOOL@
OTOOL64 = @OTOOL64@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PAMLIB = @PAMLIB@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL = @PERL@
PKG_CONFIG = @PKG_CONFIG@
PKG_CONFIG_LIBDIR = @PKG_CONFIG_LIBDIR@
PKG_CONFIG_PATH = @PKG_CONFIG_PATH@
PTHREAD_CC = @PTHREAD_CC@
PTHREAD_CFLAGS = @PTHREAD_CFLAGS@
PTHREAD_LIBS = @PTHREAD_LIBS@
RANLIB = @RANLIB@
RM = @RM@
RPCGEN = @RPCGEN@
SED = @SED@
SESSREG = @SESSREG@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SPC_INCDIR = @SPC_INCDIR@
STRIP = @STRIP@
TCL_BIN_DIR = @TCL_BIN_DIR@
TCL_INCLUDE_SPEC = @TCL_INCLUDE_SPEC@
TCL_LIB_FILE = @TCL_LIB_FILE@
TCL_LIB_FLAG = @TCL_LIB_FLAG@
TCL_LIB_SPEC = @TCL_LIB_SPEC@
TCL_PATCH_LEVEL = @TCL_PATCH_LEVEL@
TCL_SRC_DIR = @TCL_SRC_DIR@
TCL_STUB_LIB_FILE = @TCL_STUB_LIB_FILE@
TCL_STUB_LIB_FLAG = @TCL_STUB_LIB_FLAG@
TCL_STUB_LIB_SPEC = @TCL_STUB_LIB_SPEC@
TCL_VERSION = @TCL_VERSION@
TIC = @TIC@
TIRPCLIB = @TIRPCLIB@
TT_INCDIR = @TT_INCDIR@
UTMP_FILE = @UTMP_FILE@
VERSION = @VERSION@
WTMP_FILE = @WTMP_FILE@
XDMCPLIB = @XDMCPLIB@
XMKMF = @XMKMF@
XM_INCDIR = @XM_INCDIR@
XRDB = @XRDB@
XTOOLLIB = @XTOOLLIB@
X_CFLAGS = @X_CFLAGS@
X_EXTRA_LIBS = @X_EXTRA_LIBS@
X_LIBS = @X_LIBS@
X_PRE_LIBS = @X_PRE_LIBS@
YACC = @YACC@
YFLAGS = @YFLAGS@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_AR = @ac_ct_AR@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_DUMPBIN = @ac_ct_DUMPBIN@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
ax_pthread_config = @ax_pthread_config@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
runstatedir = @runstatedir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
MAINTAINERCLEANFILES = Makefile.in
@SOLARIS_FALSE@LANG = it_IT.UTF-8
@SOLARIS_TRUE@LANG = it
BUILT_SOURCES = Appmanager.sdl $(APPBUILDER) Calculator.sdl		\
    Calendar.sdl CreatAct.sdl Filemgr.sdl FPanel.sdl Help4Help.sdl	\
    Iconed.sdl Infomgr.sdl Intromgr.sdl LibDtPrint.sdl Loginmgr.sdl	\
    Mailer.sdl Printmgr.sdl PrnSetup.sdl Stylemgr.sdl Terminal.sdl	\
    Textedit.sdl

CLEANFILES = $(BUILT_SOURCES)

# install the help SDL files
sdlhelpdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)
dist_sdlhelp_DATA = $(BUILT_SOURCES)
DTDOCBOOK = $(top_srcdir)/programs/dtdocbook/doc_utils/dtdocbook2sdl
DTHELP_HTAG2 = $(top_builddir)/programs/dthelp/parser/pass2/parser/dthelp_htag2
APPMANAGER_SRCS = Appmanager/GEntity.sgm Appmanager/TOC.sgm		\
    Appmanager/Home.sgm Appmanager/Tasks.sgm Appmanager/MetaInfo.sgm	\
    Appmanager/book.sgm Appmanager/Ref.sgm

APPBUILDER_SRCS = AppBuilder/Concepts.sgm AppBuilder/MetaInfo.sgm	\
    AppBuilder/book.sgm AppBuilder/GEntity.sgm AppBuilder/Ref.sgm	\
    AppBuilder/Home.sgm AppBuilder/Tasks.sgm

CALCULATOR_SRCS = Calculator/GEntity.sgm Calculator/Ref.sgm	\
    Calculator/book.sgm Calculator/Home.sgm Calculator/TOC.sgm	\
    Calculator/MetaInfo.sgm Calculator/Tasks.sgm

CALENDAR_SRCS = Calendar/Appendix.sgm Calendar/MetaInfo.sgm	\
    Calendar/Tasks.sgm Calendar/GEntity.sgm Calendar/Ref.sgm	\
    Calendar/book.sgm Calendar/Home.sgm Calendar/TOC.sgm

CREATACT_SRCS = CreatAct/GEntity.sgm CreatAct/Ref.sgm		\
    CreatAct/book.sgm CreatAct/Home.sgm CreatAct/TOC.sgm	\
    CreatAct/MetaInfo.sgm CreatAct/Tasks.sgm

FPANEL_SRCS = FPanel/Appendix.sgm FPanel/MetaInfo.sgm	\
    FPanel/Using.sgm FPanel/GEntity.sgm FPanel/Ref.sgm	\
    FPanel/book.sgm FPanel/Home.sgm FPanel/TOC.sgm

FILEMGR_SRCS = Filemgr/Concepts.sgm Filemgr/Messages.sgm	\
    Filemgr/TOC.sgm Filemgr/GEntity.sgm Filemgr/MetaInfo.sgm	\
    Filemgr/Tasks.sgm Filemgr/Home.sgm Filemgr/Ref.sgm		\
    Filemgr/book.sgm

HELP4HELP_SRCS = Help4Help/Appendix.sgm Help4Help/Ref.sgm		\
    Help4Help/Concepts.sgm Help4Help/TOC.sgm Help4Help/GEntity.sgm	\
    Help4Help/Tasks.sgm Help4Help/Home.sgm Help4Help/book.sgm		\
    Help4Help/MetaInfo.sgm

ICONED_SRCS = Iconed/Concepts.sgm Iconed/MetaInfo.sgm	\
    Iconed/Tasks.sgm Iconed/GEntity.sgm Iconed/Ref.sgm	\
    Iconed/book.sgm Iconed/Home.sgm Iconed/TOC.sgm

INFOMGR_SRCS = Infomgr/Home.sgm Infomgr/Ref.sgm Infomgr/MetaInfo.sgm	\
    Infomgr/book.sgm

INTROMGR_SRCS = Intromgr/Appendix.sgm Intromgr/Home.sgm			\
    Intromgr/Overview.sgm Intromgr/GEntity.sgm Intromgr/Keyboard.sgm	\
    Intromgr/Windows.sgm Intromgr/Help.sgm Intromgr/MetaInfo.sgm	\
    Intromgr/book.sgm

LIBDTPRINT_SRCS = LibDtPrint/GEntity.sgm LibDtPrint/Home.sgm		\
    LibDtPrint/MetaInfo.sgm LibDtPrint/book.sgm LibDtPrint/Ref.sgm

LOGINMGR_SRCS = Loginmgr/Concepts.sgm Loginmgr/Ref.sgm		\
    Loginmgr/book.sgm Loginmgr/Home.sgm Loginmgr/TOC.sgm	\
    Loginmgr/MetaInfo.sgm Loginmgr/Tasks.sgm

MAILER_SRCS = Mailer/Messages.sgm Mailer/TOC.sgm Mailer/GEntity.sgm	\
    Mailer/MetaInfo.sgm Mailer/Tasks.sgm Mailer/Home.sgm		\
    Mailer/Ref.sgm Mailer/book.sgm

PRINTMGR_SRCS = Printmgr/GEntity.sgm Printmgr/Ref.sgm		\
    Printmgr/book.sgm Printmgr/Home.sgm Printmgr/TOC.sgm	\
    Printmgr/MetaInfo.sgm Printmgr/Tasks.sgm

PRNSETUP_SRCS = PrnSetup/GEntity.sgm PrnSetup/Home.sgm		\
    PrnSetup/MetaInfo.sgm PrnSetup/book.sgm PrnSetup/prnset.sgm

STYLEMGR_SRCS = Stylemgr/Concepts.sgm Stylemgr/MetaInfo.sgm	\
    Stylemgr/Tasks.sgm Stylemgr/GEntity.sgm Stylemgr/Ref.sgm	\
    Stylemgr/book.sgm Stylemgr/Home.sgm Stylemgr/TOC.sgm

TERMINAL_SRCS = Terminal/GEntity.sgm Terminal/Ref.sgm		\
    Terminal/book.sgm Terminal/Home.sgm Terminal/TOC.sgm	\
    Terminal/MetaInfo.sgm Terminal/Tasks.sgm

TEXTEDIT_SRCS = Textedit/GEntity.sgm Textedit/Ref.sgm		\
    Textedit/book.sgm Textedit/Home.sgm Textedit/TOC.sgm	\
    Textedit/MetaInfo.sgm Textedit/Tasks.sgm


# install the common graphics files
cmngraphdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/graphics
dist_cmngraph_DATA = $(shell find common/graphics/*)

# install the common/*.hf files
hfdirdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)
dist_hfdir_DATA = $(shell ls common/*.hf)

# Now, install any graphics assets found for each help section

# NOTE: we filter out any graphics/CDR directories here - these contain RIFF
# files.  It does not appear they are, or ever have been used, but we will
# restrain the urge to delete them all from the repo for now.

# Appmanager
appmanagerdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Appmanager/graphics
dist_appmanager_DATA = $(shell [ -d Appmanager/graphics ] && find Appmanager/graphics/* |grep -v /CDR)

# AppBuilder
appbuilderdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/AppBuilder/graphics
dist_appbuilder_DATA = $(shell [ -d AppBuilder/graphics ] && find AppBuilder/graphics/* |grep -v /CDR)

# Calculator
calculatordir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Calculator/graphics
dist_calculator_DATA = $(shell [ -d Calculator/graphics ] && find Calculator/graphics/* |grep -v /CDR)

# Calendar
calendardir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Calendar/graphics
dist_calendar_DATA = $(shell [ -d Calendar/graphics ] && find Calendar/graphics/* |grep -v /CDR)

# CreatAct
creatactdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/CreatAct/graphics
dist_creatact_DATA = $(shell [ -d CreatAct/graphics ] && find CreatAct/graphics/* |grep -v /CDR)

# Filemgr
filemgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Filemgr/graphics
dist_filemgr_DATA = $(shell [ -d Filemgr/graphics ] && find Filemgr/graphics/* |grep -v /CDR)

# FPanel
fpaneldir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/FPanel/graphics
dist_fpanel_DATA = $(shell [ -d FPanel/graphics ] && find FPanel/graphics/* |grep -v /CDR)

# Help4Help
help4helpdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Help4Help/graphics
dist_help4help_DATA = $(shell [ -d Help4Help/graphics ] && find Help4Help/graphics/* |grep -v /CDR)

# Iconed
iconeddir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Iconed/graphics
dist_iconed_DATA = $(shell [ -d Iconed/graphics ] && find Iconed/graphics/* |grep -v /CDR)

# Infomgr
infomgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Infomgr/graphics
dist_infomgr_DATA = $(shell [ -d Infomgr/graphics ] && find Infomgr/graphics/* |grep -v /CDR)

# Intromgr
intromgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Intromgr/graphics
dist_intromgr_DATA = $(shell [ -d Intromgr/graphics ] && find Intromgr/graphics/* |grep -v /CDR)

# LibDtPrint
libdtprintdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/LibDtPrint/graphics
dist_libdtprint_DATA = $(shell [ -d LibDtPrint/graphics ] && find LibDtPrint/graphics/* |grep -v /CDR)

# Loginmgr
loginmgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Loginmgr/graphics
dist_loginmgr_DATA = $(shell [ -d Loginmgr/graphics ] && find Loginmgr/graphics/* |grep -v /CDR)

# Mailer
mailerdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Mailer/graphics
dist_mailer_DATA = $(shell [ -d Mailer/graphics ] && find Mailer/graphics/* |grep -v /CDR)

# Printmgr
printmgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Printmgr/graphics
dist_printmgr_DATA = $(shell [ -d Printmgr/graphics ] && find Printmgr/graphics/* |grep -v /CDR)

# PrnSetup
prnsetupdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/PrnSetup/graphics
dist_prnsetup_DATA = $(shell [ -d PrnSetup/graphics ] && find PrnSetup/graphics/* |grep -v /CDR)

# Stylemgr
stylemgrdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Stylemgr/graphics
dist_stylemgr_DATA = $(shell [ -d Stylemgr/graphics ] && find Stylemgr/graphics/* |grep -v /CDR)

# Terminal
terminaldir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Terminal/graphics
dist_terminal_DATA = $(shell [ -d Terminal/graphics ] && find Terminal/graphics/* |grep -v /CDR)

# Textedit
texteditdir = $(CDE_INSTALLATION_TOP)/appconfig/help/$(LANG)/Textedit/graphics
dist_textedit_DATA = $(shell [ -d Textedit/graphics ] && find Textedit/graphics/* |grep -v /CDR)
all: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am $(top_srcdir)/programs/localized/templates/Italian.am $(top_srcdir)/doc/common/help/sdl-docs.am $(top_srcdir)/doc/common/help/sdl-data.am $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign doc/it_IT.UTF-8/help/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign doc/it_IT.UTF-8/help/Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles);; \
	esac;
$(top_srcdir)/programs/localized/templates/Italian.am $(top_srcdir)/doc/common/help/sdl-docs.am $(top_srcdir)/doc/common/help/sdl-data.am $(am__empty):

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-dist_appbuilderDATA: $(dist_appbuilder_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_appbuilder_DATA)'; test -n "$(appbuilderdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(appbuilderdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(appbuilderdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(appbuilderdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(appbuilderdir)" || exit $$?; \
	done

uninstall-dist_appbuilderDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_appbuilder_DATA)'; test -n "$(appbuilderdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(appbuilderdir)'; $(am__uninstall_files_from_dir)
install-dist_appmanagerDATA: $(dist_appmanager_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_appmanager_DATA)'; test -n "$(appmanagerdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(appmanagerdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(appmanagerdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(appmanagerdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(appmanagerdir)" || exit $$?; \
	done

uninstall-dist_appmanagerDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_appmanager_DATA)'; test -n "$(appmanagerdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(appmanagerdir)'; $(am__uninstall_files_from_dir)
install-dist_calculatorDATA: $(dist_calculator_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_calculator_DATA)'; test -n "$(calculatordir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(calculatordir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(calculatordir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(calculatordir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(calculatordir)" || exit $$?; \
	done

uninstall-dist_calculatorDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_calculator_DATA)'; test -n "$(calculatordir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(calculatordir)'; $(am__uninstall_files_from_dir)
install-dist_calendarDATA: $(dist_calendar_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_calendar_DATA)'; test -n "$(calendardir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(calendardir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(calendardir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(calendardir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(calendardir)" || exit $$?; \
	done

uninstall-dist_calendarDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_calendar_DATA)'; test -n "$(calendardir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(calendardir)'; $(am__uninstall_files_from_dir)
install-dist_cmngraphDATA: $(dist_cmngraph_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_cmngraph_DATA)'; test -n "$(cmngraphdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(cmngraphdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(cmngraphdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cmngraphdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cmngraphdir)" || exit $$?; \
	done

uninstall-dist_cmngraphDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_cmngraph_DATA)'; test -n "$(cmngraphdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cmngraphdir)'; $(am__uninstall_files_from_dir)
install-dist_creatactDATA: $(dist_creatact_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_creatact_DATA)'; test -n "$(creatactdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(creatactdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(creatactdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(creatactdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(creatactdir)" || exit $$?; \
	done

uninstall-dist_creatactDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_creatact_DATA)'; test -n "$(creatactdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(creatactdir)'; $(am__uninstall_files_from_dir)
install-dist_filemgrDATA: $(dist_filemgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_filemgr_DATA)'; test -n "$(filemgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(filemgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(filemgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(filemgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(filemgrdir)" || exit $$?; \
	done

uninstall-dist_filemgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_filemgr_DATA)'; test -n "$(filemgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(filemgrdir)'; $(am__uninstall_files_from_dir)
install-dist_fpanelDATA: $(dist_fpanel_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_fpanel_DATA)'; test -n "$(fpaneldir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(fpaneldir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(fpaneldir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(fpaneldir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(fpaneldir)" || exit $$?; \
	done

uninstall-dist_fpanelDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_fpanel_DATA)'; test -n "$(fpaneldir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(fpaneldir)'; $(am__uninstall_files_from_dir)
install-dist_help4helpDATA: $(dist_help4help_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_help4help_DATA)'; test -n "$(help4helpdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(help4helpdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(help4helpdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(help4helpdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(help4helpdir)" || exit $$?; \
	done

uninstall-dist_help4helpDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_help4help_DATA)'; test -n "$(help4helpdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(help4helpdir)'; $(am__uninstall_files_from_dir)
install-dist_hfdirDATA: $(dist_hfdir_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_hfdir_DATA)'; test -n "$(hfdirdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(hfdirdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(hfdirdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(hfdirdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(hfdirdir)" || exit $$?; \
	done

uninstall-dist_hfdirDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_hfdir_DATA)'; test -n "$(hfdirdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(hfdirdir)'; $(am__uninstall_files_from_dir)
install-dist_iconedDATA: $(dist_iconed_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_iconed_DATA)'; test -n "$(iconeddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(iconeddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(iconeddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(iconeddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(iconeddir)" || exit $$?; \
	done

uninstall-dist_iconedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_iconed_DATA)'; test -n "$(iconeddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(iconeddir)'; $(am__uninstall_files_from_dir)
install-dist_infomgrDATA: $(dist_infomgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_infomgr_DATA)'; test -n "$(infomgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(infomgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(infomgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(infomgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(infomgrdir)" || exit $$?; \
	done

uninstall-dist_infomgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_infomgr_DATA)'; test -n "$(infomgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(infomgrdir)'; $(am__uninstall_files_from_dir)
install-dist_intromgrDATA: $(dist_intromgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_intromgr_DATA)'; test -n "$(intromgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(intromgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(intromgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(intromgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(intromgrdir)" || exit $$?; \
	done

uninstall-dist_intromgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_intromgr_DATA)'; test -n "$(intromgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(intromgrdir)'; $(am__uninstall_files_from_dir)
install-dist_libdtprintDATA: $(dist_libdtprint_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_libdtprint_DATA)'; test -n "$(libdtprintdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(libdtprintdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(libdtprintdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(libdtprintdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(libdtprintdir)" || exit $$?; \
	done

uninstall-dist_libdtprintDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_libdtprint_DATA)'; test -n "$(libdtprintdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(libdtprintdir)'; $(am__uninstall_files_from_dir)
install-dist_loginmgrDATA: $(dist_loginmgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_loginmgr_DATA)'; test -n "$(loginmgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(loginmgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(loginmgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(loginmgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(loginmgrdir)" || exit $$?; \
	done

uninstall-dist_loginmgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_loginmgr_DATA)'; test -n "$(loginmgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(loginmgrdir)'; $(am__uninstall_files_from_dir)
install-dist_mailerDATA: $(dist_mailer_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_mailer_DATA)'; test -n "$(mailerdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(mailerdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(mailerdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(mailerdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(mailerdir)" || exit $$?; \
	done

uninstall-dist_mailerDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_mailer_DATA)'; test -n "$(mailerdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(mailerdir)'; $(am__uninstall_files_from_dir)
install-dist_printmgrDATA: $(dist_printmgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_printmgr_DATA)'; test -n "$(printmgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(printmgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(printmgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(printmgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(printmgrdir)" || exit $$?; \
	done

uninstall-dist_printmgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_printmgr_DATA)'; test -n "$(printmgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(printmgrdir)'; $(am__uninstall_files_from_dir)
install-dist_prnsetupDATA: $(dist_prnsetup_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_prnsetup_DATA)'; test -n "$(prnsetupdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(prnsetupdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(prnsetupdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(prnsetupdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(prnsetupdir)" || exit $$?; \
	done

uninstall-dist_prnsetupDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_prnsetup_DATA)'; test -n "$(prnsetupdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(prnsetupdir)'; $(am__uninstall_files_from_dir)
install-dist_sdlhelpDATA: $(dist_sdlhelp_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_sdlhelp_DATA)'; test -n "$(sdlhelpdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(sdlhelpdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(sdlhelpdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(sdlhelpdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(sdlhelpdir)" || exit $$?; \
	done

uninstall-dist_sdlhelpDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_sdlhelp_DATA)'; test -n "$(sdlhelpdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(sdlhelpdir)'; $(am__uninstall_files_from_dir)
install-dist_stylemgrDATA: $(dist_stylemgr_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_stylemgr_DATA)'; test -n "$(stylemgrdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(stylemgrdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(stylemgrdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(stylemgrdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(stylemgrdir)" || exit $$?; \
	done

uninstall-dist_stylemgrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_stylemgr_DATA)'; test -n "$(stylemgrdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(stylemgrdir)'; $(am__uninstall_files_from_dir)
install-dist_terminalDATA: $(dist_terminal_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_terminal_DATA)'; test -n "$(terminaldir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(terminaldir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(terminaldir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(terminaldir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(terminaldir)" || exit $$?; \
	done

uninstall-dist_terminalDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_terminal_DATA)'; test -n "$(terminaldir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(terminaldir)'; $(am__uninstall_files_from_dir)
install-dist_texteditDATA: $(dist_textedit_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_textedit_DATA)'; test -n "$(texteditdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(texteditdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(texteditdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(texteditdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(texteditdir)" || exit $$?; \
	done

uninstall-dist_texteditDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_textedit_DATA)'; test -n "$(texteditdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(texteditdir)'; $(am__uninstall_files_from_dir)
tags TAGS:

ctags CTAGS:

cscope cscopelist:

distdir: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) distdir-am

distdir-am: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(appbuilderdir)" "$(DESTDIR)$(appmanagerdir)" "$(DESTDIR)$(calculatordir)" "$(DESTDIR)$(calendardir)" "$(DESTDIR)$(cmngraphdir)" "$(DESTDIR)$(creatactdir)" "$(DESTDIR)$(filemgrdir)" "$(DESTDIR)$(fpaneldir)" "$(DESTDIR)$(help4helpdir)" "$(DESTDIR)$(hfdirdir)" "$(DESTDIR)$(iconeddir)" "$(DESTDIR)$(infomgrdir)" "$(DESTDIR)$(intromgrdir)" "$(DESTDIR)$(libdtprintdir)" "$(DESTDIR)$(loginmgrdir)" "$(DESTDIR)$(mailerdir)" "$(DESTDIR)$(printmgrdir)" "$(DESTDIR)$(prnsetupdir)" "$(DESTDIR)$(sdlhelpdir)" "$(DESTDIR)$(stylemgrdir)" "$(DESTDIR)$(terminaldir)" "$(DESTDIR)$(texteditdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) install-am
install-exec: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:
	-test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
	-test -z "$(BUILT_SOURCES)" || rm -f $(BUILT_SOURCES)
	-test -z "$(MAINTAINERCLEANFILES)" || rm -f $(MAINTAINERCLEANFILES)
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-dist_appbuilderDATA \
	install-dist_appmanagerDATA install-dist_calculatorDATA \
	install-dist_calendarDATA install-dist_cmngraphDATA \
	install-dist_creatactDATA install-dist_filemgrDATA \
	install-dist_fpanelDATA install-dist_help4helpDATA \
	install-dist_hfdirDATA install-dist_iconedDATA \
	install-dist_infomgrDATA install-dist_intromgrDATA \
	install-dist_libdtprintDATA install-dist_loginmgrDATA \
	install-dist_mailerDATA install-dist_printmgrDATA \
	install-dist_prnsetupDATA install-dist_sdlhelpDATA \
	install-dist_stylemgrDATA install-dist_terminalDATA \
	install-dist_texteditDATA

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-dist_appbuilderDATA \
	uninstall-dist_appmanagerDATA uninstall-dist_calculatorDATA \
	uninstall-dist_calendarDATA uninstall-dist_cmngraphDATA \
	uninstall-dist_creatactDATA uninstall-dist_filemgrDATA \
	uninstall-dist_fpanelDATA uninstall-dist_help4helpDATA \
	uninstall-dist_hfdirDATA uninstall-dist_iconedDATA \
	uninstall-dist_infomgrDATA uninstall-dist_intromgrDATA \
	uninstall-dist_libdtprintDATA uninstall-dist_loginmgrDATA \
	uninstall-dist_mailerDATA uninstall-dist_printmgrDATA \
	uninstall-dist_prnsetupDATA uninstall-dist_sdlhelpDATA \
	uninstall-dist_stylemgrDATA uninstall-dist_terminalDATA \
	uninstall-dist_texteditDATA

.MAKE: all check install install-am install-exec install-strip

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	cscopelist-am ctags-am distclean distclean-generic \
	distclean-libtool distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am \
	install-dist_appbuilderDATA install-dist_appmanagerDATA \
	install-dist_calculatorDATA install-dist_calendarDATA \
	install-dist_cmngraphDATA install-dist_creatactDATA \
	install-dist_filemgrDATA install-dist_fpanelDATA \
	install-dist_help4helpDATA install-dist_hfdirDATA \
	install-dist_iconedDATA install-dist_infomgrDATA \
	install-dist_intromgrDATA install-dist_libdtprintDATA \
	install-dist_loginmgrDATA install-dist_mailerDATA \
	install-dist_printmgrDATA install-dist_prnsetupDATA \
	install-dist_sdlhelpDATA install-dist_stylemgrDATA \
	install-dist_terminalDATA install-dist_texteditDATA \
	install-dvi install-dvi-am install-exec install-exec-am \
	install-html install-html-am install-info install-info-am \
	install-man install-pdf install-pdf-am install-ps \
	install-ps-am install-strip installcheck installcheck-am \
	installdirs maintainer-clean maintainer-clean-generic \
	mostlyclean mostlyclean-generic mostlyclean-libtool pdf pdf-am \
	ps ps-am tags-am uninstall uninstall-am \
	uninstall-dist_appbuilderDATA uninstall-dist_appmanagerDATA \
	uninstall-dist_calculatorDATA uninstall-dist_calendarDATA \
	uninstall-dist_cmngraphDATA uninstall-dist_creatactDATA \
	uninstall-dist_filemgrDATA uninstall-dist_fpanelDATA \
	uninstall-dist_help4helpDATA uninstall-dist_hfdirDATA \
	uninstall-dist_iconedDATA uninstall-dist_infomgrDATA \
	uninstall-dist_intromgrDATA uninstall-dist_libdtprintDATA \
	uninstall-dist_loginmgrDATA uninstall-dist_mailerDATA \
	uninstall-dist_printmgrDATA uninstall-dist_prnsetupDATA \
	uninstall-dist_sdlhelpDATA uninstall-dist_stylemgrDATA \
	uninstall-dist_terminalDATA uninstall-dist_texteditDATA

.PRECIOUS: Makefile


# let's try a rule here.  I don't like the backticks for running basename
# but the $(basename...) seems to add a space for some reason

%.sdl:
	SGML_SEARCH_PATH=".:.." \
		$(KSH) $(DTDOCBOOK) -H $(DTHELP_HTAG2) -L $(LANG) \
			-o $@ `basename $@ .sdl`/book.sgm

Appmanager.sdl: $(APPMANAGER_SRCS)

AppBuilder.sdl: $(APPBUILDER_SRCS)

Calculator.sdl: $(CALCULATOR_SRCS)

Calendar.sdl: $(CALENDAR_SRCS)

CreatAct.sdl: $(CREATACT_SRCS)

FPanel.sdl: $(FPANEL_SRCS)

Filemgr.sdl: $(FILEMGR_SRCS)

Help4Help.sdl: $(HELP4HELP_SRCS)

Iconed.sdl: $(ICONED_SRCS)

Infomgr.sdl: $(INFOMGR_SRCS)

Intromgr.sdl: $(INTROMGR_SRCS)

LibDtPrint.sdl: $(LIBDTPRINT_SRCS)

Loginmgr.sdl: $(LOGINMGR_SRCS)

Mailer.sdl: $(MAILER_SRCS)

Printmgr.sdl: $(PRINTMGR_SRCS)

PrnSetup.sdl: $(PRNSETUP_SRCS)

Stylemgr.sdl: $(STYLEMGR_SRCS)

Terminal.sdl: $(TERMINAL_SRCS)

Textedit.sdl: $(TEXTEDIT_SRCS)

# this sets the LANG variables
# this does all the work
# install all of the extra assets

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
